Run the same prompt up to 3 more times, adding the argument --tag N, where N is the iteration
number, which modifies the output filename. Review the results of each of these additional
iterations, and if their scores are all 100%, output `<promise>DONE</promise>` and stop iterating.

Otherwise, continue iterating on the previous task.
