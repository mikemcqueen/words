#!/bin/bash

if [[ $# -ne 1 ]]; then
    echo "Usage: $0 \"some question\""
    exit 1
fi

curl -X POST http://localhost:8000/yesno \
     -H "Content-Type: application/json" \
     -d "{\"text\": \"$1\"}"
